import React from 'react';

const HeaderDescription = (props) => (
  <div>
    <p>** Now Tech Jobs in Thailand only ** (Singapore’s coming soon!) Finding jobs shouldn’t be that hard. Let’s crowed sourcing them ! Employer can own a list describing all of its job . The post will be checked before being displayed and will be display for one month</p>
  </div>
);

export default HeaderDescription;
